const About = ({ data }) => {
	const {
		heading,
		historyHeading,
		historyText,
		carouselImages,
		renovationHeading,
		renovationText,
	} = data ? data : {};
	return (
		<div>
			<div></div>
		</div>
	);
};

export default About;
